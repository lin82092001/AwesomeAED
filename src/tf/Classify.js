import React, { } from 'react'
import {
    StyleSheet,
} from 'react-native'
import { Container, Text } from 'native-base'

export const Classify = () => {
    return (
        <Container>
            <Text>Classify</Text>
        </Container>
    )
}